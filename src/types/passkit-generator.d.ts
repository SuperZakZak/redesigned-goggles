declare module 'passkit-generator' {
  // Minimal typings to satisfy TypeScript; extend as needed
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  export function createPass(options: any): Promise<any>;
}
